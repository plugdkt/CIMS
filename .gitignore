*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/framework/phpstan
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
/composer.phar
/build
/fonts

# Raw, un-curated source export for the chemicals:import command — the curated,
# reviewed result lives at database/data/chemicals_import.csv (tracked). This file
# is a large one-off working copy, not meant to be committed.
/download.csv
/storage/download.csv

# Raw HR/roster exports for users:import-lab-assignments — real personal data
# (names/usernames/emails), never committed. The command persists only the
# derived username->lab_id mapping into the database, never the source file.
/storage/app/_staff_imports/
