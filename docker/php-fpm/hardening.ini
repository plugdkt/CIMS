; T-053 (ZAP baseline scan): PHP's default X-Powered-By header leaks the
; exact PHP version to every response — a low-risk info-disclosure finding,
; standard hardening to turn off in any real deployment.
expose_php=Off
